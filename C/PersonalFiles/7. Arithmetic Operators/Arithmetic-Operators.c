#include <stdio.h>

int main() {

    // arithmetic operators 

    // +  (Addition)
    // -  (Subtraction)
    // *  (Multiplication)
    // /  (Division)
    // %  (Modulus)
    // ++ (Increment)
    // -- (Decrement)

    int x = 5;
    int y = 2;

    int z = x % y;

    printf("%d\n", z);

    int a = 8;
    int b = 1;

    a++;

    int c = a+b;

    printf("%d", c);

    return 0;
}